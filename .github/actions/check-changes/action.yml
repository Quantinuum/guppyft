name: Detect changed files.
description: Checks if any relevant files have changed in the current pull request or commit. Always returns true if running on the default branch, to ensure all changes are thoroughly checked.

# The file filters are defined in `.github/change-filters.yml`.
#
# Requires
# ```
# permissions:
#   pull-requests: read
# ```

# Set job outputs to values from filter step
# These outputs are always true when running after a merge to main, or if the PR has a `X-run-thorough-ci-tests` label.
outputs:
  rust:
    description: "Need to run rust tests"
    value: ${{ steps.override.outputs.out == 'true' || steps.change-filters.outputs.rust == 'true' }}
  python:
    description: "Need to run python tests"
    value: ${{ steps.override.outputs.out == 'true' || steps.change-filters.outputs.python == 'true' || steps.change-filters.outputs.encoder == 'true' }}
  extensions:
    description: "The extension definitions have changed"
    value: ${{ steps.override.outputs.out == 'true' || steps.change-filters.outputs.extensions == 'true' }}

runs:
  using: composite
  # Check if changes were made to the relevant files.
  # Always returns true if running on the default branch, to ensure all changes are thoroughly checked.
  steps:
    - name: Override label
      shell: bash
      id: override
      run: |
        echo "Label contains X-run-thorough-ci-tests: $OVERRIDE_LABEL"
        if [ "$OVERRIDE_LABEL" == "true" ]; then
          echo "Overriding due to label 'X-run-thorough-ci-tests'"
          echo "out=true" >> $GITHUB_OUTPUT
        elif [ "$DEFAULT_BRANCH" == "true" ]; then
          echo "Overriding due to running on the default branch"
          echo "out=true" >> $GITHUB_OUTPUT
        fi
      env:
        OVERRIDE_LABEL: ${{ github.event_name == 'pull_request' && contains( github.event.pull_request.labels.*.name, 'X-run-thorough-ci-tests') }}
        DEFAULT_BRANCH: ${{ github.ref_name == github.event.repository.default_branch }}
    - if: steps.override.outputs.out != 'true'
      uses: dorny/paths-filter@v4
      id: change-filters
      with:
        filters: .github/change-filters.yml
