name: Build wheels

on:
  push:
    branches:
      - main
      - "release-please--*"
      - mr/chore/release-workflow
    tags:
      - "v*"
  workflow_dispatch:

env:
  UV_EXTRA_INDEX_URL: "https://${{ secrets.QTNM_ARTIF_SW_READ_USER }}:${{ secrets.QTNM_ARTIF_SW_READ_TOKEN }}@quantinuumsw.jfrog.io/artifactory/api/pypi/pypi_local/simple"
  PYTHON_VERSION: "3.12"

jobs:
  build_wheels:
    name: ${{ matrix.os }} wheels
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # macos-15-intel runners are x86_64
        os: [ ubuntu-latest, ubuntu-22.04-arm, macos-latest, macos-15-intel, windows-latest ]

    steps:
      - name: Checkout the repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - name: Set up uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true
      - uses: actions/cache@v5
        if: runner.os != 'Linux'
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install hugrenv
        uses: Quantinuum/hugrverse-env/install-hugrenv-action@main
        with:
          packages: llvm,tket
          lockfile: hugrenv.lock

      - name: Build guppy-ft wheel
        uses: pypa/cibuildwheel@v3.4.1
        with:
          output-dir: 'wheelhouse'

      - uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*guppy-ft*.whl

  release:
    name: Release wheels
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [ build_wheels ]
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: wheelhouse
          merge-multiple: true

      - name: Update GitHub release with artifacts
        uses: softprops/action-gh-release@v3
        with:
          files: wheelhouse/*guppy-ft*.whl

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Upload guppy-ft wheels to SW artifactory
        run: |
          uvx twine upload --verbose --non-interactive wheelhouse/*guppy-ft*.whl
        env:
          TWINE_REPOSITORY_URL: "https://quantinuumsw.jfrog.io/artifactory/api/pypi/pypi_local/"
          TWINE_USERNAME: ${{ secrets.QTNM_ARTIF_SW_WRITE_USER }}
          TWINE_PASSWORD: ${{ secrets.QTNM_ARTIF_SW_WRITE_TOKEN }}