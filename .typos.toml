[default.extend-words]
inouts = "inouts"
ba = "ba"

[type.changelog]
extend-glob = ["CHANGELOG.md"]
# Release entries contain Git hashes that can resemble misspelled words.
extend-ignore-re = ["\\b[0-9a-f]{7,40}\\b"]
